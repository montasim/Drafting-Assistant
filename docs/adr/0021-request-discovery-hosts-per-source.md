---
status: accepted
---

# Request discovery host access per enabled source

Groq and each Discovery Source origin will be declared as optional host access and requested only when the user enables the corresponding capability or source. Disabling a source removes its access, no discovery origin receives a content script, and discovery permissions remain independent from LinkedIn permission so the feature adds no LinkedIn page execution or activity.
