# Use a canonical side-panel viewport

Pixel parity with the approved prototype will be measured at a 400 × 820 CSS-pixel side-panel viewport. The same tokenized components must remain usable from 320 pixels upward and at wider user-selected panel sizes; visual-regression tests will protect the canonical viewport while responsive tests protect overflow, focus visibility, and layout integrity at the supported extremes.
