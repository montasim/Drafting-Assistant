# Build a new extension codebase

Thoughtline will be implemented as a new Chrome extension in the `Thoughtline` workspace rather than by modifying the existing `/ai-comments` extension. This gives the approved product model and modular architecture a clean foundation; the existing extension may be consulted as a behavioral reference but remains untouched and is not a runtime or source dependency.
