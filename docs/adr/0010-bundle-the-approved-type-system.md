# Bundle the approved type system

Thoughtline will bundle Charter for display text, Noto Sans for interface text, and Roboto Mono for utility text rather than relying on operating-system font availability. The Tailwind theme will expose these as semantic font tokens so the 400 × 820 visual baseline remains deterministic and the extension remains fully offline-capable at the presentation layer.
